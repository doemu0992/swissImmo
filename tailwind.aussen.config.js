/* Mieterportal, Bewerbungsformular, öffentliche Formulare, Fehlerseiten:
   Tailwind in der Voreinstellung, OHNE die Petrol-Palette.

   Das ist Absicht und übernommen aus fw/_tailwind_palette.html: Der
   Palette-Baustein lag nie auf diesen Seiten. Sie jetzt beim Umstellen
   nebenbei umzufärben wäre eine Gestaltungsentscheidung — und die gehört
   nicht in eine Etappe, die nur Fremdquellen entfernt. */
module.exports = { content: require('./tailwind.inhalt.js') };
